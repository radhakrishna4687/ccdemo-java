version: 0.2 

phases:
  install: 
    commands: 
      - echo Nothing to do the install phase....
  pre_build:
     command:
      - echo Build started on `date'
      - mvn install 
  post_build: 
    command: 
      - echo build completed on `date`
artifacts:
  file:
    - 'target/ccdemo.war'     